# Interface Audit

A dated, cumulative record of design-quality reviews across Primitiv — the
Figma component sets and the registry stylesheets together.

Each pass runs one skill from the `interfaces` set and appends a new dated
section. **Append, never edit a previous pass** — a finding that was fixed
gets ticked in place with the commit that fixed it, so the document reads as
a history rather than a snapshot.

The companion Figma page is **`Interface Audit — 2026-08-15`** (page id
`1847:13528`), which carries the visual findings and live before/after demos
built from real component instances. Every pass adds a numbered section to
both surfaces.

## Fixed so far

Landed against these findings, newest first:

| Commit | Finding | Pass |
| --- | --- | --- |
| `990ce2f2` | Prose measure cap — an opt-in `measure` modifier at a calibrated `51ch`, guarded by a real character count | `better-typography` / `better-layout` |
| `4096fc63` · `33a11417` | Heading letter-spacing — a per-density Context member, plus two new ramp steps, a table guard and a browser test | `better-typography` |
| `8ca536e0` · `3996e39e` | Button optical icon inset — new token family, plus a browser test | `better-ui` |
| `208023c9` | `from-font` underline metrics, `text-wrap: balance`/`pretty` | `better-typography` |
| `9b0f810a` | Dark link state ramp ran toward black — all four states, plus a direction guard | `better-colors` |
| `5b091aea` | Safe-area clearance on Drawer, per docked side | `better-layout` |
| `e0be6466` | One edge token for the dropdown-family overlay panels | `better-ui` |
| `80fb3a50` | Tactile press feedback — `scale(0.96)` on Button / Chip / Breadcrumb Overflow / Carousel | `better-ui` |
| `219b0cc6` | Drawer scroll containment, and font smoothing at the root | `better-accessibility` / `better-typography` |
| `230da21c` | Image outlines — Avatar / Card / Carousel / Figure | `better-ui` |
| `a05445fc` · `d45a3a66` · `f5032434` | `tabular-nums` — **working**, not inert (the "no `tnum`" verdict was corrected by measurement); extended to the Avatar Group counter, with Badge's heading-face numerals left as a design question | `better-typography` |
| `768b0f35` | Forced-colors focus indicator, 27 blocks across 26 components, plus a guard | `better-accessibility` |
| `d44ade7f` | Dark `action/link/foreground/default` and `content/muted` contrast, plus test coverage | `better-colors` |

Deliberately parked: the `border/default` alias convention, the `table`
numeric-cell API, and the two Figma text-style findings in Pass 06 below.

**The font pair is no longer one of them.** It was parked as blocking the
`tabular-nums` fix; measurement on 2026-08-18 reversed that — Asta Sans carries
`tnum` and the fix works. What remains is narrower: only the *heading* face lacks
the feature, which affects Badge's numerals alone. The live pairing preview
(`0198b27d`) also had the numeric requirement applied to **both** slots when only
the body slot needs it, hiding most display and condensed faces from the heading
list; that slot now offers 11 more.

One build gap closed along the way (`140ed09c`): the workbench's committed
`primitiv-base.css` had silently missed this session's three typography fixes,
because no workflow emits that file and no gate checks it. Both committed copies
are now synced by hand. The underlying gap — two generated files with no
regeneration path — is still open.

## Passes

| Date | Skill | Findings | Verdict |
| --- | --- | --- | --- |
| 2026-08-15 | `better-ui` | 6 medium · 3 low | Needs changes |
| 2026-08-15 | `better-accessibility` | 1 high · 4 medium · 2 low | **Block** |
| 2026-08-15 | `better-typography` | 4 medium · 3 low | Needs changes |
| 2026-08-15 | `better-layout` | 3 medium · 3 low | Needs changes |
| 2026-08-15 | `better-colors` | 4 medium · 3 low | Needs changes |
| — | `better-writing` | not yet run | — |

## How colour findings land

Two constraints govern every colour recommendation in this document.

**Notation.** Recommendations are written in **OKLCH**, matching Harmoni's
oklch-first model and the direction the system is moving in. **Figma currently
holds hex**, so mirroring a value into a Figma variable means converting —
`oklch(0 0 0 / 0.1)` becomes `#0000001a`. That is a **representation gap, not
drift**; recorded here so a later token-sync pass doesn't "fix" one side to
match the other.

**Provenance.** Every palette colour is **generated by the Harmoni Rust engine
and pushed through the Figma plugin** — the ramps are engine output, not
hand-authored values. So a colour finding is only actionable if it can be
expressed as one of:

1. **An engine input change** — brand hue/chroma, the lightness curve, neutral
   tinting, the alpha curve. This is the route for anything affecting a ramp's
   shape or a whole family.
2. **A semantic re-binding** — pointing an Intent or Context token at a
   different step of an existing generated ramp. No engine change; the ramp is
   untouched and only the alias moves.
3. **A deliberate non-palette literal** — a value that is *not* a system colour
   and should never enter the ramp. Pass 01's image outline is the reference
   case: it must be pure black/white at 10% precisely because it must *not*
   pick up the engine's hue-tinted neutrals. Rare, and each one needs
   justifying in the stylesheet.

**"Change this hex" is never a valid recommendation** — it would be overwritten
on the next generate. Any colour finding must be routed to one of the three
above.

## Not every fix can start in Figma

The colour pipeline above is Figma-first because the ramps genuinely originate
there. Some fixes cannot follow it at all, and `tabular-nums` is the clearest
case — worth recording so the asymmetry isn't mistaken for an oversight.

**Figma's plugin API cannot set OpenType features.** Probed on all three
families: `openTypeFeatures` is **read-only** ("no setter for property"),
`getRangeOpenTypeFeatures` reports only *applied* features rather than available
ones, and `setRangeOpenTypeFeatures` does not exist. `listAvailableFontsAsync`
carries nothing but `fontName`. So there is no scripted path, and the sync
plugin can never carry this.

**And the CSS fix relies on inheritance, which Figma has no equivalent for.**
`font-variant-numeric` is inherited, so one declaration on a component root
covers every numeric part beneath it. Figma applies OpenType features per text
node or per text style — there is no cascade. That leaves three options, and
none of them is a clean mirror:

| | Cost | Problem |
| --- | --- | --- |
| Per text node | **251 nodes** (Pagination 87, Data Table 76, Stepper 65, Badge 20, Carousel 3) | Impractical by hand, impossible by script |
| On the shared `Body / *` + `Label / *` styles | 40 styles (5 sizes × 4 density modes × 2 families) | **Wrong on design grounds** — prose wants *proportional* figures; this would turn tabular figures on for all body copy |
| Leave Figma as-is, document it | 0 | The Figma file is a slightly inexact spec for these three components |

**The third is the right answer**, and it is the same call Card already documents
three times over: where CSS expresses something Figma cannot, record the
divergence rather than distorting one side to match. Figma showing proportional
digits on a Pagination mock is a known, harmless inexactness.

**~~Resolved 2026-08-17: Asta Sans does not expose `tnum`.~~ WRONG — see the
correction below.** Checked in Figma's type-settings panel — no figure-style
control appears for the family, and Figma does surface that control for fonts that
have the feature. Not independently confirmed in a browser (no browser access in
the agent environment), so this rests on the Figma panel rather than a rendered
measurement.

**~~Consequence: the shipped declaration is inert for the default theme.~~**
~~Browsers do not synthesise tabular figures the way they synthesise bold — with
no `tnum` in the font, `font-variant-numeric: tabular-nums` silently does
nothing.~~

> ### Correction (2026-08-18, measured in Chromium)
>
> **Asta Sans *does* implement `tnum`, and the fix works.** The caveat above —
> "rests on the Figma panel rather than a rendered measurement" — was the right
> caveat, and the measurement reverses the conclusion. At 100px:
>
> | Face | `1111111111` | `8888888888` | With `tabular-nums` |
> | --- | --- | --- | --- |
> | Asta Sans (body) | 488 | 600 | **603.67 / 603.67 — uniform** |
> | Khand (heading) | 278 | 455 | 278 / 455 — no change |
>
> So the body face draws proportional figures by default (a real 23% swing, as
> filed) **and** carries the feature to fix them. Khand genuinely has no `tnum`,
> which is probably what the Figma panel was showing — and Figma resolves the
> *heading* family for many of these specimens.
>
> **Confirmed on the real components, not just the face:** `.primitiv-pagination`
> and `.primitiv-data-table` both resolve `"Asta Sans"` with
> `font-variant-numeric: tabular-nums` in effect and measure a digit run
> identically at `96.59` either way. `.primitiv-carousel__progress-text` is a
> contract part a consumer applies, so the kitchen-sink renders no instance to
> measure — its rule is identical in kind.
>
> **Everything below this box is therefore moot**: there is no inertness to
> accept, no numeric readout to move to mono, and no reason to reconsider the
> text font on these grounds. The `table` numeric-column gap that came out of
> investigating option 2 is still a real finding, but it is about **column
> alignment**, not digit shift — a genuine downgrade in urgency.
>
> **Guarded** by `apps/kitchen-sink/e2e/tabular-figures.spec.ts` (3 specs), since
> this is a font-pair coupling exactly like the Prose measure: a body face without
> `tnum` would silently restore the shifting. One spec asserts body text is
> **still** proportional, so the suite cannot pass by the font being uniform on
> its own instead of by our declaration working. Verified red by deleting
> Pagination's declaration — only that component failed.
>
> **Why it was wrong is worth keeping.** Checking a font's features through a
> design tool's UI and inferring browser behaviour is the same class of mistake as
> the other reversals in this audit: reasoning about a rendered property without
> rendering it.
>
> **The sweep was also incomplete**, which only showed up once I stopped asking
> "which components did we declare this on" and started scanning for components
> that *render* digits. Two were missed, both fixed in `f5032434`:
>
> | Component | Renders | Face | Outcome |
> | --- | --- | --- | --- |
> | `avatar-group__counter` | `+3`, `+2`, `+1` | Asta Sans | **Fixed** — the declaration works |
> | `badge` | `12`, `+135`, `-169` (35 instances) | **Khand** | Declared, but **inert** — see below |
>
> Ruled out by the same scan rather than assumed: **Progress** renders no digits
> at all, and **Stepper**'s markers are static per step, so neither is a changing
> value. Injecting a digit run measures a font's metrics, not what a component
> displays — a distinction that would have produced two more phantom findings.
>
> **Open design question — Badge's numerals.** Badge is very often a count or a
> delta, and `--primitiv-badge-font-family` resolves the **heading** family, which
> under the shipped pair is Khand and has no `tnum` (9px swing per four digits at
> 100px). The declaration is right and stays — the family is a knob, so a consumer
> whose heading face carries the feature, or who points that knob at
> `--primitiv-font-family-text`, gets aligned digits for free — but making them
> align under the *shipped* pair is a decision about which face a numeric badge is
> set in, not a CSS fix. Three ways out: point Badge's family at the text family
> (changes its label's face too), accept the shift, or choose a heading face with
> `tnum`. **Recorded, not decided.** The current state is pinned by a test that
> asserts the measured inertness, so whichever way it goes, the change announces
> itself.

**It is still the right declaration, and it stays.** Registry components are
copied surfaces and `--primitiv-font-family-text` is a token consumers override,
so any consumer whose text font carries tabular figures gets the benefit with no
work. The declaration is dead only against *our* default font, not in general —
removing it would take the fix away from every consumer who can use it.

**What remains open is the default theme.** Three options, in rough order of how
much they cost:

1. **Accept the drift and document it.** Cheapest; leaves a real 23% digit-width
   swing in Pagination and Data Table.
2. **Set the numeric readouts in `--primitiv-font-family-mono`** (JetBrains Mono,
   measured at 0px spread). For a Data Table's numeric columns this is arguably
   an improvement rather than a compromise — aligned columns are the point. For
   Pagination it changes the look noticeably.
3. **Reconsider the default text font.** Correct in the long run if numeric
   alignment matters across the system, and far out of scope for a finding.

**Option 2 turned out not to be implementable as scoped** (investigated
2026-08-17). The recommendation was mono on Data Table's *numeric columns* —
but there is no way to identify one:

- `data-table` imports `TableCell` / `TableHeader` from the **`table`**
  component, so the data cells are not its own.
- `table`'s `TableCellProps` is just the primitive's props — no `align`, no
  `numeric` — and its stylesheet sets `text-align: start` for every cell.
- The `align: "end"` prop that *does* exist, documented "for numeric columns",
  is on `DataTableSortButton` — the column **header**, not the cells. And
  `__region--end` is the **toolbar** region (it carries `bar-gap` and holds
  filter fields), not a cell container. Both were misread as numeric-cell
  affordances during the audit.

So a consumer right-aligns a numeric column with their own class or inline
style, and the component has no notion of it.

**That is its own finding: `table` has no numeric-column affordance.** A table
component with no way to mark a numeric column is a real gap independent of any
font question — it is what a `numeric` cell modifier would exist for
(`text-align: end` + `tabular-nums`, and a font hook if wanted).

**Deliberately not built yet.** Adding a cell API is a proper feature — contract,
recipe, tsx, styles, scss, README, kitchen-sink, Figma variant — and it should
not be rushed as a side effect of a font limitation. Especially because the
font-pairing question may remove the need for it entirely: **a text font that
carries `tnum` makes the already-shipped declaration work everywhere with zero
API change.** That makes the font decision the highest-leverage move here, and
an argument for leaving the API alone until it is settled.

## Two committed generated files that nothing regenerates

Found while shipping the font-smoothing fix, and worth its own entry because it
will bite again.

`primitiv-base.css` is emitted from a hand-authored asset
(`crates/primitiv-emit/assets/base.{css,scss}`, embedded verbatim via
`include_str!`). Changing the asset does **not** update the committed copy at
`apps/kitchen-sink/src/styles/primitiv/primitiv-base.css`: the *Regenerate
tokens* workflow only runs `primitiv tokens --out …/tokens.css`, which does not
write the base sheet. It ran green after the change and committed nothing.

The copy is byte-identical to the asset, so syncing it is a straight file copy —
exactly what the generator produces — but nothing *checks* that, and the
*Token drift* gate covers only `tokens.css`.

The same gap exists for `apps/workbench/src/primitiv-tokens.css`, flagged
earlier: a committed generated file that no workflow emits and no gate checks,
which is why it still carried the pre-fix dark contrast values.

**Two candidate fixes, neither taken here:** extend *Regenerate tokens* (and the
drift gate) to cover both files, or retire the copies that no longer need to
exist. Either is a decision about the build, not a finding about the interface.

**Verifying a route-1 change needs CI.** The Rust toolchain is not available in
the agent environment (org policy, not a missing binary), so an engine-input
change cannot be validated where the design work happens — you can reason about
it, but you cannot see the ramp it produces.

That gap is now closed by the **Harmoni ramp audit** workflow
(`.github/workflows/ramp-audit.yml`), which runs
`crates/harmoni-core/examples/ramp-audit.rs` and publishes the result. Run it
by hand (`workflow_dispatch`) on whatever branch carries the engine change, read
the summary in the run, and download the `ramp-audit` artefact to diff a
before-run against an after-run. It reports:

1. **Reproduction** — whether each ramp still regenerates from its seed and
   matches `palette.json` step for step.
2. **Quality metrics** — hue span, light-end lightness spacing, chroma peak:
   the three measurements the findings below turn on.
3. **The full per-step L/C/H table**, so two runs are directly comparable.

Quality is **reported, never gated** — a gate would block the very commits
trying to improve a ramp. Only reproduction can fail the run, and only when
explicitly asked (`strict`), because a palette that no longer regenerates from
its seeds always means something is out of sync.

This also fixed a gap the audit surfaced incidentally: **the palette had no
recorded inputs**. The seeds were entered interactively in the Figma plugin and
only the output was ever committed, so the ramps were not reproducible from the
repo at all. They now live in `packages/tokens/harmoni-seeds.json`, and the
workflow verifies they are correct rather than trusting them.

Until a run exists, treat the route-1 findings below as **diagnosed but
unverified**: the measurement of the *current* ramp is solid, the proposed
remedy is not yet tested. Route-2 and route-3 findings are unaffected — they are
pure token edits and are verifiable without the engine.

---

# 2026-08-15 — `better-ui`

Sixteen principles, checked against both halves of the system. Roughly half
are static and decidable in Figma (concentric radius, optical alignment,
shadows-vs-borders, image outlines, icon treatment); the other half exist
only in CSS (interruptible transitions, scale-on-press, `will-change`,
transition specificity, theme-switch suppression). A Figma-only review would
silently skip eight of them, so both surfaces were swept.

**Scope:** 62 registry components · `packages/tokens/src` resolved across 4
density modes × 5 size slots · `packages/icons` (48 glyphs) ·
`apps/workbench/src/pages` · Figma file `1Nh5ffky0lYEw0MzXoqQVy`.

## Summary

| | Count | Notes |
| --- | ---: | --- |
| High | 0 | Nothing makes an interaction misleading or unresponsive. |
| Medium | 6 | Craft and consistency; most affect many components at once. |
| Low | 3 | Isolated polish, concentrated in the icon layer. |
| Verified clean | 6 | Including 120/120 on focus-ring concentricity. |
| Not applicable | 3 | Motion-library principles; the registry ships no motion dependency. |

The headline is good news. Focus-ring geometry is **exactly concentric across
every component, size and density mode in the system**, and the motion layer
is more disciplined than most codebases manage. The findings cluster around
images, press feedback, and one inconsistency in how floating surfaces define
their edge.

## Findings

### Optical alignment

| Severity | Location | Before | After | Why |
| --- | --- | --- | --- | --- |
| ~~MEDIUM~~ **FIXED** (`8ca536e0`, tested `3996e39e`) | `registry/components/button/styles.css:51` | `padding-inline: var(--primitiv-button-padding-inline)` — one value, both edges, no adjustment when an icon is present | Asymmetric via `:has()`: pull the icon side in by a new density-scaled `framed-control/{size}/icon-optical-inset` (≈2px at `md`, 4px at `xl`) | An icon is a dense glyph filling its box to the edge; a text label carries natural side bearing. With equal padding the icon side reads heavier and the label appears pushed right. **The system already has this concept** — `alert.{size}.icon-offset-top` is a density-scaled optical-alignment token added for exactly this reason on Alert's icon, so the fix has in-house precedent rather than introducing a new idea. Affects Button, Split Button, and any framed control with a leading or trailing slot.<br><br>**Fixed 2026-08-17.** Created in Figma first as `framed-control/{size}/icon-optical-inset` (1–8px across 5 sizes × 4 density modes, derived as 25% of `padding-inline` snapped down the space ladder), mirrored into `context.json`, regenerated, then consumed by Button.<br><br>**Figma cannot show the behaviour, and that is structural.** The correction applies only when an icon is present, but `Leading Icon` is a BOOLEAN property and a Figma boolean drives only `visible`/`characters`/`mainComponent`, never a layout value. A variant axis would multiply the set past 125 variants; baking the inset into every variant would wrongly shift the icon-less ones. So the variables are the source for the *values* and the conditional application is CSS-only — recorded in the Button set description, same category as Card's three documented divergences.<br><br>**Proved in a real browser**, since jsdom implements neither `:has()` matching nor `calc()` over custom properties: `apps/kitchen-sink/e2e/button-optical.spec.ts`, 6 specs. Four assert the inset applies (including that the amount equals the token, resolved at runtime rather than hard-coded); two assert it does *not* over-apply to label-only and icon-only buttons. Verified red by deleting the two `:has()` rules — 4 fail, and the 2 symmetry guards correctly still pass, which is what makes them guards. |

> Confirmed independently by Simon before the sweep reached it. Demo on the
> Figma page, section 01 — a live `xl` Button beside a copy with the inset
> applied.

### Concentric border radius

| Severity | Location | Before | After | Why |
| --- | --- | --- | --- | --- |
| MEDIUM | `packages/tokens/src/context.json` → `card-media/{size}/radius-inset`<br>`registry/components/card/styles.css:304` | Media inset by the full `card.padding` (16px at `comfortable/md`) but drawn at radius **8** inside a card of radius 10 | Concentric value is `max(0, 10 − 16)` = **0**. Either square the inset media, or record the deviation as intentional in the Card description and stylesheet header | Inner radius currently tracks the card's radius one notch down the ladder and ignores padding entirely. Because padding always exceeds the radius, an inset photo reads nearly as round as the card holding it — at **0 of 20** size × density combinations is it concentric, and the gap widens as density loosens (`spacious/xl`: drawn 16, concentric 0). |
| LOW | `packages/tokens/src/context.json` → `dropdown/{size}/item/radius` | Panel inset is a flat 4px (`dropdown/styles.css:175`), so `item.radius` should be `panel.radius − 4`. `xs` is 2 too round at every density; `spacious/xl` is 4 too square | Bring the 8 outliers onto `panel.radius − 4` | **12 of 20** combinations are already exactly concentric, so the rule is understood — the extremes were simply clamped to the radii ladder. Low because a 2–4px error only shows on a hovered row's corner. |

### Image outlines

| Severity | Location | Before | After | Why |
| --- | --- | --- | --- | --- |
| ~~MEDIUM~~ **FIXED** (`230da21c`) | `avatar` · `card` · `carousel` · `figure` | No outline on any image surface anywhere in the registry | `outline: 1px solid oklch(0 0 0 / 0.1); outline-offset: -1px;` and `oklch(1 0 0 / 0.1)` under `[data-theme=dark]` | Pale-edged photography dissolves into a light surface. Visible in the Figma Avatar set: initials and placeholder avatars carry a neutral fill so their circle reads crisply, while photo avatars — studio portraits on a near-white backdrop — lose their top-left edge entirely. Same component, same size, inconsistent shape definition purely because there is an image in it.<br><br>**Use raw literals, not tokens, for this one colour.** The neutral ramp is deliberately hue-tinted (`neutral-300` is `#a8aeb6`, a cool blue-grey), and a tinted outline picks up the surface beneath it and reads as dirt on the image edge. This is the one place in the stylesheet where a literal beats the token. |

### Shadows for elevation, borders for structure

| Severity | Location | Before | After | Why |
| --- | --- | --- | --- | --- |
| ~~MEDIUM~~ **FIXED, and substantially rediagnosed** (`e0be6466`) | `dropdown/styles.css:115` · `select/styles.css:232` | Dropdown and Select drew `border-default`; Combobox drew `border-subtle` — the same conceptual surface, two tokens | Align on `border-subtle`, which is what Figma's `Dropdown / Panel` stroke specifies | **The original diagnosis was wrong in both direction and remedy.** It called this a three-way inconsistency and proposed removing the border with Popover as the exemplar. Reading the Figma source settles it differently: `Dropdown / Panel` binds **`border/subtle`**, `Modal` binds **`border/default`**, and `Popover` has **no stroke at all**. So the design deliberately scales the edge with the overlay's weight — subtle for a light transient panel, default for a heavy one, none for Popover — which is three answers for three different things, not an inconsistency. **Combobox was right; Dropdown and Select were the drift** `CLAUDE.md` already flagged.<br><br>**Removing the border would have been actively wrong.** `shadow.color.*` is `absolute-black`-based and does not invert, so at 4–8% black on a dark surface the elevation is nearly invisible and this edge is the only thing defining the panel. Popover is borderless because a stroke would seam across its arrow's base — a constraint, not an example to follow.<br><br>**One tension recorded, not resolved:** `border-subtle` measures 1.79:1 against white against `border-default`'s 2.24:1, so this makes the edge fainter at the spot pass 05 flagged for non-text contrast. Figma is the design source and says `subtle`, so code now matches it; whether that edge should be stronger is a design question, not a drift. |

### Scale on press

| Severity | Location | Before | After | Why |
| --- | --- | --- | --- | --- |
| ~~MEDIUM~~ **FIXED** (`80fb3a50`) | `button` · `chip` · `breadcrumb-overflow` · `carousel` | No `:active` transform anywhere in the registry — zero matches across every stylesheet | `:active { scale: 0.96; }` with `transition-property: scale`, a `static` escape hatch, and a `prefers-reduced-motion` guard | Press changed colour only. **Fixed 2026-08-17**, and the scope is smaller than 62 components because Button is composed almost everywhere — modal/drawer/popover close, alert dismiss, split-button halves, code-block copy all inherit it. Only Chip, Breadcrumb Overflow and the Carousel controls draw their own bare `<button>` and needed it directly.

Two implementation calls worth keeping. It is gated behind `prefers-reduced-motion: no-preference`, which is only safe *because* the `:active` colour change is not gated — motion is additive, so a reduced-motion user still gets a press state. And it is disabled per instance via `--primitiv-<c>-press-scale: 1`, matching the registry's custom-property escape-hatch idiom, rather than the `static` prop the principle suggests: a prop would need contract, recipe and wrapper changes on a copied surface for the same effect.

**Selection controls were deliberately left out** — Tabs, Segmented Control, Toggle Group, Pagination. Pressing those selects rather than acts, and they are high-frequency, which is exactly where motion restraint applies. |

### Suppress transitions on theme switch

| Severity | Location | Before | After | Why |
| --- | --- | --- | --- | --- |
| MEDIUM | `apps/workbench/src/pages/ElevationExample/ElevationExample.tsx:33`<br>`apps/workbench/src/pages/DesignSystemTestExample/DesignSystemTestExample.tsx:46` | `setTheme(t => t === "light" ? "dark" : "light")` flips `data-theme` with nothing guarding the transitions | Inject `*,*::before,*::after{transition:none !important}`, force a reflow, remove on the next frame | The flip re-points colour, background, border and box-shadow on every element at once, and the registry carries **112** transition declarations that all fire together. The switch smears instead of snapping. Bites hardest on the Elevation specimen, whose entire purpose is judging shadows against both grounds. |

### Icons

| Severity | Location | Before | After | Why |
| --- | --- | --- | --- | --- |
| LOW | `packages/icons/src/IconBase.tsx:22`<br>`packages/icons/src/icons/*.tsx` | Strokes baked into filled path geometry at a fixed `24` viewBox, then scaled by `size` — effective stroke lands near 1.0px at `size=16`, 1.5px at 24, 2.0px at 32 | Accept and document, or add optically-corrected 16px glyphs for the sizes that drift furthest | Stroke weight tracks the icon's *size* rather than the weight of the text beside it, so a small icon next to semibold text comes out lighter than the principle wants. Inherent to the stroke-to-outline build recipe — a known-limits note rather than a defect. |
| LOW | `packages/icons/src/icons/` (48 glyphs) | One outline glyph per concept; no filled counterparts | Add fill variants for the handful used as active-state markers | Outline-as-default / fill-as-active is simply unavailable to consumers — a selected tab or nav item cannot mark itself with a weight change and must rely on colour alone. `currentColor` is correctly wired throughout, so only the fill variant is missing. |

## Verified clean

Worth recording explicitly — four of these are the result of deliberate
engineering and would be easy to erode by accident later.

- **Focus-ring concentricity.** 30 focus-ring stems × 4 density modes,
  resolved through the alias chain. Every one satisfies `gap = base + 2` and
  `ring = base + 4` against `focus.ring.offset` / `focus.ring.width`.
  **120/120, zero drift.** Slider and Radio use `radii.full` throughout,
  correct for a circular thumb.
- **Transition only what changes.** 112 transition declarations across the
  registry; **zero** instances of `transition: all` or
  `transition-property: all`. Every one names its properties.
- **Interruptible animations.** Only 10 `@keyframes` in 62 components, all
  correctly scoped: one-shot directional panel enters (navigation-menu,
  stepper), an infinite loader (progress), and scroll-driven `linear both`
  effects (carousel parallax, cover-flow). Interactive state changes all use
  transitions. Every animation carries a `prefers-reduced-motion` guard.
- **Sparing `will-change`.** No declarations at all; the only match in the
  registry is a carousel comment explaining its deliberate absence.
- **One SVG, recoloured per state.** `IconBase` sets `fill="currentColor"`
  for all 48 glyphs; states come from CSS colour. No per-state assets.
- **Shadow colour neutrality.** `shadow.color.{soft,medium,strong}` are pure
  black at 4/6/8% — not tinted neutrals — so elevation stays honest on both
  grounds. The same discipline the image-outline fix needs.

## Not applicable

`initial={false}` on `AnimatePresence`, staged enter stagger, and subtle exit
transitions all assume a motion library the registry deliberately does not
ship — there is no `motion` or `framer-motion` dependency. Recorded as
not-applicable rather than passing.

## Verification

- **Token arithmetic, resolved not assumed.** Parsed `context.json`,
  `primitives.json` and `interaction.json`, followed every `{alias}` chain to
  a literal, and computed the concentric relationships across all 4 density
  modes × 5 size slots. Findings quote resolved pixel values, not token names.
- **Two numbers were corrected mid-audit.** The Dropdown row inset was first
  taken from `panel.padding-block`; reading `dropdown/styles.css:175` showed
  the inline axis uses a flat `space-4`. Recomputing moved that finding from
  "20/20 wrong" to **12/20 correct** and dropped it from Medium to Low. The
  Card finding was likewise held back until `card/styles.css:304` confirmed
  `margin: var(--primitiv-card-padding)` — the media really is inset by the
  full padding.
- **Registry swept in full.** All 62 component stylesheets grepped for
  `transition`, `@keyframes`, `will-change`, `:active` transforms, image
  selectors and outline declarations. Every count in this document comes from
  those sweeps.
- **Figma inspected live** over the Desktop Bridge (probe 3 ms). Rendered the
  Card set (`1444:37322`, 30 variants) and the Avatar set (`433:7944`, 30
  variants). The Avatar render is the direct evidence for the image-outline
  finding; the Card render confirms the inset-media geometry.

### Still to verify

- **Motion was reviewed as source, not replayed** at 10% speed in the
  browser's Animations panel. The theme-switch smear is predicted from the
  source, not observed.
- **Optical alignment was not measured on rendered controls.** The Button
  finding is derived from the symmetric `padding-inline` and confirmed by
  Simon from experience; the exact per-size inset values are a proposal and
  want a browser check before they land as tokens.
- **Icon centring inside Icon Button** (a square control with a single
  centred glyph) is the other classic optical case and was not checked.

## Verdict

**Needs changes.** Six Medium and three Low findings remain; no High. Nothing
here is misleading or unresponsive — the foundations are unusually
disciplined, and the focus-ring result is something most design systems never
achieve at all.

If only one thing gets done: the **image outline**. It is a handful of lines,
it affects Avatar, Card, Carousel and Figure at once, and the Avatar render
shows the system already looks inconsistent with itself without it.

If two: add `framed-control/{size}/icon-optical-inset`, since it is already a
known problem and the token family it belongs to is well established.

---

# 2026-08-15 — `better-accessibility`

Fourteen principles, walked keyboard-first then screen-reader-first across the
headless library, the registry stylesheets, and the kitchen-sink surface.

**Scope:** `packages/react/src` (headless semantics, focus management) · 62
registry components · `packages/tokens/src` resolved across 4 density × 5 size
· `apps/kitchen-sink`.

## Summary

| | Count | Notes |
| --- | ---: | --- |
| High | 1 | Focus indicators vanish entirely in forced-colors mode. |
| Medium | 4 | Hit areas, scroll containment, a missing autoplay control, page structure. |
| Low | 2 | Disabled styling and one stray selector. |
| Verified clean | 9 | The semantics layer is in genuinely good shape. |

The library's *semantics* are strong — native elements throughout, correct
live-region primitives, real label wiring, APG keyboard patterns, and a
`@media (hover: hover)` gate on **24 of 24** hover-styled components. The
findings are concentrated in **rendering modes and sizing**, not in markup.

## Findings

### Visible focus rings

| Severity | Location | Before | After | Why |
| --- | --- | --- | --- | --- |
| ~~**HIGH**~~ **FIXED** (`768b0f35`) | `registry/components/*/styles.css` — 27 blocks across 26 components | `:focus-visible { outline: none; box-shadow: 0 0 0 <offset> <surface>, 0 0 0 <offset+width> <ring>; }` — **zero** `forced-colors` blocks exist across all 62 components | Add to the shared ring recipe:<br>`@media (forced-colors: active) { outline: 2px solid Highlight; outline-offset: 2px; }` | **Browsers strip `box-shadow` entirely in forced-colors mode** (Windows High Contrast). The `outline: none` survives. The net result is that a keyboard user in forced-colors gets **no visible focus indicator anywhere in the system** — every button, input, tab, tree row and menu item. The two-layer box-shadow ring is otherwise excellent work (it follows the control's radius and stays concentric at every size, verified 120/120 in pass 01); it just needs a forced-colors fallback beside it. This is one shared recipe, so it is one fix, not 28. |

### Minimum hit area

| Severity | Location | Before | After | Why |
| --- | --- | --- | --- | --- |
| MEDIUM | `packages/tokens/src/context.json` → `framed-control/{size}/height` | `dense/xs` = **16px**, `dense/sm` = **20px**, `compact/xs` = **20px** — square icon-only controls at these sizes are below the floor | Either raise the three to 24px, or document them as opt-in with a spacing requirement, or extend the hit area with a pseudo-element while the visible control stays small | WCAG 2.5.8 (AA) sets a 24×24 CSS-pixel floor. **3 of 20** density × size combinations fall under it, and an Icon Button is square so height *is* the target. The 24px exceptions (spacing, inline, equivalent-control, essential) may cover specific usages, but as shipped defaults these three are a documented risk rather than a considered choice. The remaining 17 combinations pass, and everything at `comfortable` and `spacious` clears 24 comfortably. |

### Trap and restore focus

| Severity | Location | Before | After | Why |
| --- | --- | --- | --- | --- |
| ~~MEDIUM~~ **FIXED for Drawer** (`219b0cc6`); Modal half withdrawn | `registry/components/drawer/styles.css:181` | Drawer's `__body` scrolls (`overflow: auto`) with no `overscroll-behavior` | `overscroll-behavior: contain` — **Drawer only** | Dropdown, Combobox, Select and Carousel all contain their own scroll; Drawer was the one overlay that did not, so reaching the end of its body moved the page behind it.

**The Modal half was wrong.** Modal has no `overflow`, no `max-block-size` and no height cap of any kind — so it has no independently scrollable region for the scroll to chain out of, and nothing to contain. That absence is arguably its own finding (a Modal with long content has no internal scroll and will grow past the viewport rather than scrolling its body) but it is a different problem from this one, and not filed here. The focus trap itself is sound: Modal uses a native `<dialog>` (which inerts the background) plus a boundary-only Tab-wrap in `useModalContent.ts` and focus restore on close. |

### Motion and timed UI

| Severity | Location | Before | After | Why |
| --- | --- | --- | --- | --- |
| MEDIUM | `registry/components/carousel/carousel.tsx` | The headless layer ships `Carousel.PlayPauseTrigger` (and correctly flips `aria-live="off"` while autoplay runs), but the **styled surface exports 11 parts and none of them is a play/pause control** | Add a `CarouselPlayPauseTrigger` part with matching styles | A consumer who runs `primitiv add carousel` and enables autoplay has no styled pause control available — they would have to reach past the registry surface into the headless package to build one. Autoplaying content needs a visible pause control regardless of the reduced-motion preference. The headless design is right; the styled surface just does not expose it. |

### Structure is navigation

| Severity | Location | Before | After | Why |
| --- | --- | --- | --- | --- |
| MEDIUM | `apps/kitchen-sink/src/App.tsx:1613` | Primary content is an `<article>`; there is no `<main>` landmark, and no skip link, while `<nav aria-label="Page sections">` precedes all content | Wrap the content region in `<main>` and make "Skip to content" the first focusable element | The table of contents is a real `<nav>` with an accessible name — which is exactly the case the principle covers: repeated navigation preceding the content means a keyboard user tabs through the entire TOC before reaching anything. On a page this long that is a lot of tab stops. Heading structure is otherwise correct: exactly one `<h1>`, properly nested below it. Scoped to the kitchen-sink, not the library. |

### Disabled states and selectors

| Severity | Location | Before | After | Why |
| --- | --- | --- | --- | --- |
| LOW | `registry/components/button/styles.css:287` and the other components using the shared disabled recipe | `[data-disabled] { opacity: var(--primitiv-opacity-50); }` | Replace the opacity with a **semantic re-binding** — point `action/*/disabled/foreground` and `/background` at existing steps of the generated neutral ramp (route 2 above, no engine change) — plus a `forced-colors` branch using `GrayText` | Opacity-only disabled styling multiplies text contrast by 0.5, which takes otherwise-passing label text well below any threshold, and opacity is unreliable as a state signal in forced-colors mode. Low rather than Medium because disabled text is exempt from WCAG contrast minimums — but "exempt" is not the same as "legible", and this is the system's default for every control. Note this is deliberately *not* a request for new colour values: the steps already exist in the Harmoni-generated ramp, so only the aliases move. |
| LOW | `registry/components/select/styles.css:758` | `.primitiv-select__item:focus` | `:focus-visible`, or `[data-highlighted]` to match the sibling components | Listbox and Combobox both document that their rows never match `:focus` (focus stays on the frame, with `aria-activedescendant` carrying the cursor) and correctly key off `[data-highlighted]`. Select using bare `:focus` for the same conceptual state is inconsistent with its two closest siblings, and bare `:focus` shows on pointer interaction too. |

## Verified clean

The semantics layer is the strongest part of the system and is worth recording
in detail, because most of it would be easy to erode.

- **Hover gated for touch — 24 of 24.** Every component with `:hover` rules
  wraps them in `@media (hover: hover)`. No sticky hover states after a tap.
- **Native elements throughout.** No `<div onClick>`, no `role="button"`
  anywhere in the registry. Actions are `<button>`, navigation is `<a href>`.
- **Correct live-region pairing.** Both primitives exist and are used for the
  right thing: `Alert` renders `role="alert"` (implicit `aria-live="assertive"`)
  for urgent errors, `Status` renders `role="status"` (implicit polite) for
  routine updates. Most systems ship only one and misuse it.
- **Label and description wiring.** `Field` wires `htmlFor`, `aria-describedby`
  and `aria-invalid` through a shared id, so help text and error text are
  properly associated rather than merely adjacent.
- **Accessible names on icon-only controls.** Chip's remove button, Breadcrumb
  Overflow's trigger and Modal's close all carry `aria-label`.
- **Focus trap and restore.** Modal uses a native `<dialog>` — the background
  is genuinely inert, not just visually covered — with a boundary-only Tab-wrap
  and focus restored to the trigger on close.
- **Reduced motion.** Every one of the 10 `@keyframes` and all transition-driven
  open/close animations carry a `prefers-reduced-motion` guard (carried over
  from pass 01, re-confirmed here).
- **Autoplay semantics.** Carousel flips `aria-live="off"` while autoplay runs,
  so auto-advance does not spam a screen reader, and `PlayPauseTrigger` throws
  if used without autoplay enabled — a correct API guard.
- **Text resize.** Font sizes emit in `rem`, so browser text-size settings and
  200% zoom both work rather than being pinned by `px`.

## Verification

- **Focus mechanism traced to source.** Confirmed the ring is
  `outline: none` + two-layer `box-shadow` by reading `button/styles.css:276-283`,
  then grepped all 62 stylesheets for `forced-colors` and `-ms-high-contrast`
  — **zero matches**. The HIGH finding follows from the CSS spec behaviour of
  `box-shadow` under forced colors, which is well-established, not from a
  rendered test.
- **Hit areas computed, not eyeballed.** Resolved `framed-control/{size}/height`
  through the alias chain for all 4 density modes × 5 sizes and compared each
  against the 24px floor.
- **Semantics swept across both layers.** Grepped `packages/react/src` and
  `registry/components` for live-region roles, `aria-label`, `htmlFor`,
  `aria-describedby`, `aria-invalid`, `role="button"`, `<div onClick>`,
  `overscroll-behavior`, `@media (hover: hover)` and bare `:focus`.

### Still to verify

- **No screen reader was run.** Every semantic finding here is from source, not
  from NVDA / JAWS / VoiceOver output. The live-region behaviour in particular
  (principle 11 notes that dynamically inserted alerts have inconsistent
  support) needs testing against real assistive tech before being called done.
- **No forced-colors rendering test.** The HIGH finding is derived from spec
  behaviour; confirming it takes a Windows High Contrast session or Chrome's
  forced-colors emulation.
- **Keyboard walkthrough not performed interactively.** APG conformance was
  read from the headless implementations and their tests, not driven by hand.
  NavigationMenu's deliberate non-roving tabindex (correct for a disclosure
  nav) is the kind of decision that source review catches and a naive automated
  check would flag wrongly.
- **200% zoom / 320px reflow unverified.** `rem` font sizing is a good signal
  but not proof; the kitchen-sink's fixed-position TOC is the most likely
  reflow casualty.

## Verdict

**Block.** One HIGH finding remains: focus indicators disappear entirely in
forced-colors mode, across the whole system. That makes the interface
unusable by keyboard for anyone relying on high-contrast rendering, and it is
a single shared fix rather than 28 separate ones.

Everything else is Medium or below, and the semantics layer underneath is
genuinely strong — this is a rendering-mode gap in an otherwise well-built
accessibility story, not a systemic problem.

---

# 2026-08-15 — `better-typography`

Nineteen principles, checked against the emitted type scale, the base element
sheet, and the 62 registry stylesheets.

**Scope:** `packages/tokens/src` (the resolved type scale) ·
`primitiv-base.css` (the global typographic foundation) · 62 registry
components · `apps/kitchen-sink`.

## Summary

| | Count | Notes |
| --- | ---: | --- |
| High | 0 | |
| Medium | 4 | Numerals, measure, mobile input sizing, font smoothing. |
| Low | 3 | Heading tracking, underline metrics, wrap hints. |
| Verified clean | 7 | The scale itself is the strongest part. |

**The scale is genuinely well built** — headings descend monotonically with
line-height ratios that loosen as size drops, everything emits in `rem`, and
`text-transform` + `letter-spacing-wide` are applied exactly where the
principles want them. The findings are all *missing* refinements rather than
wrong decisions, and four of the seven are one-line additions with a token
that already exists.

## Findings

### Tabular numbers on changing values

| Severity | Location | Before | After | Why |
| --- | --- | --- | --- | --- |
| MEDIUM | `registry/components/pagination` · `data-table` · `carousel` (ProgressText) | **Zero** `font-variant-numeric` declarations across all 62 stylesheets | `font-variant-numeric: tabular-nums` — on the root for Pagination and Data Table (the property inherits), on `__progress-text` for Carousel | Proportional digits have different widths, so every value that updates shifts the layout around it. **Fixed 2026-08-17** (`a05445fc`). Measured in Figma at 100px: Asta Sans spreads **46px** between `1111` and `8888` (23%), Khand **66px** (54%, it is condensed so its `1` is very narrow), JetBrains Mono 0px. Khand being the worst matters because it is the *label* font — numeric Badges and Stepper markers are the sharpest case, not the tables.<br><br>**Scope corrected from six components to three.** Progress and Slider render no numeric text at all — they pass values as attributes and a CSS custom property, the bar is purely visual. Stepper's marker is an `aria-hidden` dot, not a numeral. The original finding counted components that conceptually have numbers rather than checking what they render. |

### Cap the measure

| Severity | Location | Before | After | Why |
| --- | --- | --- | --- | --- |
| ~~MEDIUM~~ **FIXED** (`990ce2f2`) | `registry/components/prose/styles.css` (54 lines, flow spacing only)<br>`registry/components/container/styles.css:41` | Prose sets **no** width constraint of any kind. Container caps at breakpoints — `lg` is `64rem` = **1024px** | An **opt-in** `measure` modifier on Prose, capping at `51ch` — a calibrated value, not the conventional one | These are the two components a consumer composes for long-form reading, and neither caps line length. Container's cap is a *layout* width and correctly so; the measure cap belongs on Prose, whose entire purpose is long-form content.<br><br>**Fixed 2026-08-18, and the estimate was wrong in both directions.** This row originally said "roughly 128 characters", which was arithmetic from `1024px ÷ 16px`, not a reading — and this pass's own verification notes admit "nothing was read at real content lengths". Measured in Chromium: at Container `lg` a paragraph runs **137** characters, and even at `sm` (608px) it runs **90**. Both worse than filed.<br><br>**`51ch`, not the `68ch` proposed.** `ch` is the width of the `0` glyph, and the ratio between that and the average character advance varies by typeface. For the shipped body face (Asta Sans, 16px): `1ch` = 9.656px, average advance = 7.216px, ratio **0.747** — an unusually wide `0`. So the conventional `65ch` would run about **87** characters, past the range, and the proposed `68ch` about 91. `51ch` lands at ~68. The first calibration run was thrown away because it measured a **fallback font** (`document.fonts.ready` was not awaited, giving ratio 0.818) — a 20% error that would have shipped a wrong constant.<br><br>**Opt-in, and the kitchen-sink is the proof.** `.primitiv-flow` is a rhythm context, not a text column: `apps/kitchen-sink/src/App.tsx` wraps the *entire page* in one `<Prose asChild>`, every grid, table and card inside it. A default cap would have collapsed the lot. So `measure` is a modifier a consumer applies to running text, with `--primitiv-prose-measure` to retune or switch off (`none`).<br><br>**Deliberately not a design token.** A measure is only meaningful in `ch`, and neither tier can express it — the emitter writes lengths as `rem`, Figma frame widths are px. A token would either drop the font-relative unit (losing the one property that makes a measure survive a size change) or mean something different on each platform. It is a component custom property with its calibration recorded in the stylesheet, the README and the test.<br><br>**Guarded by measurement** (`apps/kitchen-sink/e2e/prose-measure.spec.ts`, 3 specs): the capped column's median line is ≥ 60 characters and no line exceeds 75; an uncapped flow still fills its parent; the custom property still narrows it. This is the guard for the coupling nothing else catches — **change the body font pair and the ratio moves**, so the measure drifts out of range with the stylesheet untouched, and this test is what will say so. Verified red by deleting the cap rule: 2 fail, and the opt-in guard correctly still passes. |

### Inputs at 16px on mobile

| Severity | Location | Before | After | Why |
| --- | --- | --- | --- | --- |
| MEDIUM | `registry/components/input/styles.css:81` (`--xs`)<br>`registry/components/input/styles.css:90` (`--sm`) | `--xs` resolves `body-xs` = **12px**; `--sm` resolves `body-sm` = **14px** | **Needs a decision, not a silent fix** — either size the input up on mobile (changes how it looks on small screens), or hold `font-size: 16px` and render the designed size with `transform: scale()`, compensating width and line-height | iOS Safari zooms the entire page when a focused input's text is below 16px, and the user is left zoomed in afterwards. `md`/`lg`/`xl` are all ≥16px and safe; only the two small slots are affected. The skill is explicit that the two remedies look different and the design should choose, so this is flagged rather than fixed. |

### Font smoothing on the root

| Severity | Location | Before | After | Why |
| --- | --- | --- | --- | --- |
| ~~MEDIUM~~ **FIXED** (`219b0cc6`) | `crates/primitiv-emit/assets/base.{css,scss}` | No `-webkit-font-smoothing` / `-moz-osx-font-smoothing` anywhere in the sheet or the registry | `-webkit-font-smoothing: antialiased; -moz-osx-font-smoothing: grayscale;` once on the root | On macOS text renders heavier than intended without it. This one has an unusually clear home: `primitiv-base.css` describes itself as "the global typographic foundation… shipped as part of the foundation, not installed per-component", which is exactly the root-level, once-only placement the principle calls for. Nothing about the current structure makes this awkward — it is simply absent. |

### Letter-spacing by size

| Severity | Location | Before | After | Why |
| --- | --- | --- | --- | --- |
| ~~LOW~~ **FIXED** (`4096fc63`, tested `33a11417`) | `packages/tokens/src/context.json` → `heading.h1`–`h6`<br>`primitiv-base.css:176` | The heading token family carries `font-family`, `font-size`, `font-style`, `font-weight` and `line-height` — but **no `letter-spacing` member**, so no heading sets tracking | A `heading.h{n}.letter-spacing` member whose alias differs **per density mode**, plus two new ramp steps | `--primitiv-letter-spacing-tight` (`-0.075rem`) and `-tighter` (`-0.15rem`) **already exist in the token layer** and are used nowhere. An `h1` at 48px with default tracking reads loose — large type is exactly where negative tracking earns its place.<br><br>**Fixed 2026-08-17.** Built in Figma first (2 primitives + 6 Context variables + bound on 24 heading text styles), mirrored into `primitives.json` / `context.json`, regenerated, then consumed on `h1`–`h6` in `base.css` / `base.scss`.<br><br>**The proposed shape was wrong, and the reason matters.** "Bind `tight` at h1–h2, easing to normal by h4" assumes one value per level. But tracking here is **absolute** (`rem`, like its `font-size`/`line-height` siblings) while `heading.h1` is **18px in Dense and 88px in Spacious** — a single `tight` would read as `-0.067em` and `-0.014em` respectively, the first unreadable and the second invisible. So the source is a 24-cell table: each mode aliases the step landing near `-0.02..-0.03em` at *its* size, easing to `normal` by roughly 24px. Every cell verified against its own font size; all land in `-0.017..-0.030em`.<br><br>**Two new ramp steps were needed, not a new value bent to fit.** `letter-spacing/normal` (`0`) and `/snug` (`-0.6`, continuing the ramp's existing halving `2.4 → 1.2 → 0.6`). Without `snug`, Compact h1 at 32px had to take `tight` and would have landed at `-0.0375em`, outside the band — adding the missing rung beat distorting a cell to reach the rungs that existed. Literals stay in the primitive tier; all 24 Context cells are aliases.<br><br>**Figma held it as PERCENT, which would have silently diverged.** Every one of the 188 text styles carries `letterSpacing: {unit: PERCENT, value: 0}`, and a bound FLOAT adopts the node's existing unit — so `-2.4` would have meant `-2.4%` (≈`-0.024em`) in Figma while the emitter writes `-0.15rem` (`-2.4px`). The unit is pinned to PIXELS before each bind so both platforms mean the same thing.<br><br>**Guarded twice.** `packages/tokens/src/heading-tracking.test.ts` (18 tests) asserts every cell exists, sits in the band relative to its own font size, and is monotone in both directions — the check against a cell copied from the wrong column, which is invisible by inspection since `{letter-spacing.tighter}` looks as plausible at h6 as at h1. `apps/kitchen-sink/e2e/heading-tracking.spec.ts` (3 specs) then proves it reaches an element, because the wiring failure is silent: a `var()` naming a property that does not exist drops the declaration and leaves the heading at `normal` with no error. That spec asserts the property is *declared* as well as used — without it, an unwired level passes by reading `0 === 0`. Verified red by breaking one var name. |

### Underlines from the font

| Severity | Location | Before | After | Why |
| --- | --- | --- | --- | --- |
| ~~LOW~~ **FIXED** (`208023c9`) | `primitiv-base.css:92`, `:364` | `text-decoration-line: underline` with no position or thickness control | Add `text-underline-position: from-font` and `text-decoration-thickness: from-font` | Default underlines sit wherever the browser decides rather than where the typeface's own metrics put them, and at `Asta Sans`' body sizes that usually means clipping descenders. `text-decoration-skip-ink` defaults to `auto` so the worst case is avoided, but position and thickness are still browser guesses. Note the `<abbr>` treatment at line 98 is already correct — dotted underline as the extra-info hint, exactly per the principle.<br><br>**Fixed 2026-08-17** on links and `<ins>`/`<u>`. `<abbr>` was left alone deliberately — its dotted underline is a tuned affordance signalling extra information, not a browser default to override. |

### Wrap deliberately

| Severity | Location | Before | After | Why |
| --- | --- | --- | --- | --- |
| ~~LOW~~ **FIXED** (`208023c9`) | 61 of 62 registry stylesheets | `text-wrap: balance` appears **once** (`miller-columns/styles.css:295`); `text-wrap: pretty` appears **nowhere** | `balance` on headings (base sheet `h1`–`h6`), `pretty` on descriptions — Card, Alert, EmptyState, Tooltip | A two-line heading currently breaks wherever it lands, commonly leaving one short word on line two; a card description can end on a single-word orphan. Both are one declaration and neither affects long-form text, where both should stay off. Miller Columns having it proves the convention is understood — it just was not generalised.<br><br>**Fixed 2026-08-17**: `balance` on the base sheet's `h1`–`h6`, `pretty` on the ten `__description` parts. Neither applied to long-form copy — `balance` caps out after a few lines, and both are the wrong tool for a paragraph. |

## Verified clean

- **The heading scale is textbook.** Sizes descend monotonically (48 · 40 · 32
  · 28 · 24 · 20) and the line-height ratio *loosens* as size drops — 1.167 at
  h1 through 1.4 at h6 — which is exactly right: large type needs tighter
  leading, small type needs more. No level can overpower its parent.
- **Everything emits in `rem`.** Font sizes and line-heights both, so browser
  text-size preferences and 200% zoom work rather than being pinned by `px`.
- **No raw-tag misuse.** Zero `font-variation-settings` and zero
  `font-feature-settings` anywhere — the system uses real CSS properties, which
  keep working when a non-variable fallback renders.
- **Uppercase is styled, not typed.** All four uppercase labels (dropdown,
  context-menu, listbox, select) use `text-transform: uppercase` in CSS, so
  copy stays in natural case.
- **Small caps get their tracking.** Those same labels bind
  `--primitiv-letter-spacing-wide`, which is the half of principle 8 that most
  systems miss. (The heading half is the LOW finding above.)
- **The `<abbr>` hint is correct.** `text-decoration-style: dotted` marks
  extra-information words, precisely the convention the principle describes.
- **Truncation exists where rows can overflow** — combobox, listbox, select,
  tree and miller-columns all handle it rather than letting text escape.

### A considered deviation, not a finding

Line-heights are **absolute (`rem`), not unitless**. The principle prefers
unitless so leading scales with the element's font size. Here the tokens ship
as font-size/line-height *pairs* and every ratio is correct, which is a
defensible system-level choice (Tailwind does the same). Worth knowing the one
failure mode it creates: a consumer who overrides only
`--primitiv-input-font-size` — without also overriding
`--primitiv-input-line-height` — gets broken leading, where a unitless value
would have adapted. Documenting the pairing requirement is probably enough.

## Verification

- **Type scale resolved, not read.** Followed `heading.h1`–`h6` and
  `body.xs`–`xl` through the alias chain to literal `rem` values, then computed
  every line-height ratio to check the hierarchy.
- **Measure quantified.** Read Container's `lg` cap (`64rem`) and divided by
  `body-md` (16px) to get the ~128-character figure, after confirming Prose's
  stylesheet is 54 lines with no width constraint at all.
- **Registry swept in full.** All 62 stylesheets grepped for
  `font-variant-numeric`, `text-wrap`, `text-transform`, `letter-spacing`,
  `text-decoration`, `text-overflow` / `line-clamp`, `font-variation-settings`
  and `font-feature-settings`.

### Still to verify

- ~~**Nothing was read at real content lengths.**~~ **Closed 2026-08-18** for the
  measure finding, which was indeed arithmetic and indeed wrong: real copy laid
  out in Chromium gives 137 characters at Container `lg`, not the 128 estimated,
  and 90 even at `sm`. The correction is in the fixed row above, and the character
  count is now a standing test rather than a one-off reading. The **wrapping**
  findings are still only from absent declarations.
- **No macOS rendering comparison** was made for the font-smoothing finding.
- **The typefaces were not evaluated.** Khand (headings) / Asta Sans (text) /
  JetBrains Mono are loaded from Google Fonts and the pairing was not assessed
  for contrast or suitability — that is a design judgement rather than a code
  finding, and worth a deliberate look if it has never had one.

## Verdict

**Needs changes.** Four Medium and three Low, no High.

The scale underneath is the strongest foundation of the three passes so far —
nothing here is *wrong*, and several findings are one declaration against a
token that already exists. Start with `tabular-nums`: six components, one
property, and it removes visible layout jitter that a user notices without
being able to name it.

---

# 2026-08-15 — `better-layout`

Ten principles, checked against the 62 registry stylesheets with particular
attention to direction-independence, since the system already ships RTL
affordances (Avatar Group has an explicit `Direction ltr|rtl` axis;
NavigationMenu carries an RTL keymap) and half-done RTL is worse than none.

**Scope:** 62 registry components · the breakpoint scale · `Container`, `Grid`,
`Drawer` and `Carousel` as the layout-owning components.

## Summary

| | Count | Notes |
| --- | ---: | --- |
| High | 0 | |
| Medium | 3 | Text growth, container-level adaptation, safe areas. |
| Low | 3 | Breakpoint provenance and two physical-direction leftovers. |
| Verified clean | 4 | Logical properties are essentially perfect. |

**The direction-independence work is exemplary** and worth stating plainly:
**zero** `margin-left`, `margin-right`, `padding-left`, `padding-right`,
`border-left` or `border-right` across all 62 stylesheets, against 588
`padding-inline`, 91 `inset-inline`, 47 `margin-inline` and 14 `border-inline`.
Most systems claiming RTL support do not survive that grep. The findings are
about *growth and context*, not direction.

## Findings

### Plan for growth and clipping

| Severity | Location | Before | After | Why |
| --- | --- | --- | --- | --- |
| ~~MEDIUM~~ **WITHDRAWN** — the finding was wrong | `button/styles.css:50` and five siblings | `block-size: var(--primitiv-<component>-height)` — a **fixed** height on six text-bearing controls | **No change. `min-block-size` does not address the problem I described** | **Withdrawn 2026-08-17.** `block-size` is *height*; a longer translation needs *width*, and the width is already `auto`. The reasoning is documented in the code I was proposing to change (`button/styles.css:84–92`): `white-space: nowrap` raises the label's min-content width to its full rendered width, and a flex item's default `min-width: auto` **is** its min-content size — so the button grows to fit a longer label and cannot be squeezed narrower than its own text. There is no clipping.

The Figma mockup that appeared to show clipping only did so because I forced a fixed frame width, which is not what CSS does. `min-block-size` would only help if a consumer overrode `font-size` above what the fixed height accommodates — a far narrower case than the one filed, and a definite height is arguably correct for a framed control since it is what makes a row of them align. The `nowrap` itself is correct and should stay (a wrapped button label reads as broken); it is the fixed `block-size` that removes the escape route. One property per component, and the visual result is identical at English lengths. |

### Hold structure until it breaks

| Severity | Location | Before | After | Why |
| --- | --- | --- | --- | --- |
| MEDIUM · **RE-SCOPED, NOT A STYLESHEET FIX** | `container/styles.css:114`, `:119` · `grid/styles.css:116`, `:136` · 11 `@media` blocks system-wide | Component-level adaptation is driven by **viewport** width. There are **zero** container queries in the system — the one `@container` match is a comment in `stepper/styles.css:502` noting the same custom-property limitation applies there | Not the swap proposed below — this is RFC 0009 §5's deferred work, and it needs a DOM decision before any CSS can be written | The *diagnosis* holds: Container and Grid are precisely the components a consumer nests inside other layouts, and a `Container` in a 320px sidebar on a 1440px screen still resolves its `lg` gutter because it asks the window how wide *it* is. The *prescription* was wrong, in two ways found on 2026-08-18 when I went to implement it.<br><br>**1. It cannot be a swap, and the failure would be silent.** A container query resolves against the nearest **ancestor** container; an element is never its own query container. Measured in Chromium, not inferred: with `container-type: inline-size` on a 320px element and a matching `@container` block, the rule applied to a child and **not** to the element declaring the containment. `Grid` renders a *single* element — and `asChild` can make that element the consumer's own — so it has no ancestor to query, and switching `@media` → `@container` would leave every tier class matching nothing. Existing grids would collapse to their base column count with no error. Nor can the two coexist as a progressive enhancement: a non-matching container query cannot *undo* a matching `@media` rule, so the viewport value would still win in exactly the narrow-parent case the fix is for.<br><br>**2. Fixing it component-locally is the duplication RFC 0022 §4 warned about.** That section already designed responsive `Grid`/`Container` as `@container` blocks and deferred it specifically so it would land on the *same* mechanism as RFC 0009 §5's container-query density model — "building it twice, once for density, once for layout, would be a real drift risk". What shipped is a hybrid: RFC 0022 §4's option (b) API shape (`columns={{ base: 1, md: 2, lg: 3 }}`) on option (a)'s viewport mechanism. Re-mechanising it here, ahead of §5, is precisely the collision the RFC set out to avoid.<br><br>**One real bug fixed on the way** (`docs/rfcs/0009…§5`): that section's illustrative snippet put `container-type` and the queried rule on the same class, so the block it recommends could never have applied. Corrected with the measurement above. Whoever picks up §5 needs to decide where the container element comes from *first* — that is the actual blocking decision, and it is an API question, not a CSS one.<br><br>The note about literal breakpoint values (a custom property cannot appear in an `@media` condition — RFC 0025 §4) is sound and unrelated; it applies equally to `@container`. |

### Content bleeds, controls float

| Severity | Location | Before | After | Why |
| --- | --- | --- | --- | --- |
| ~~MEDIUM~~ **FIXED** (`5b091aea`) | `drawer/styles.css:94`, `:123`, `:133` | `position: fixed` with `inset-block: 0` / `inset-inline: auto 0`, and **no** `env(safe-area-inset-*)` anywhere in the registry | Pad the drawer's edges with `max(<token>, env(safe-area-inset-<side>))` | Drawer pins to physical viewport edges, so its content sat under a notch or home indicator — a bottom drawer's footer actions being the worst case. **Fixed 2026-08-17** with padding rather than inset, so the surface still bleeds to the edge and only the content clears it.<br><br>Necessarily **per docked side**: `env(safe-area-inset-*)` is a *viewport*-level value, not a per-element one, so padding a right-docked drawer by the left inset would add space for an edge it never touches. Each variant pads only the three edges it actually meets. The `env()` names have no logical equivalents, which is why these are physical properties in a file that is otherwise logical throughout.<br><br>**One dependency worth knowing:** these resolve to 0 unless the page sets `viewport-fit=cover` on its viewport meta — iOS reports no insets without it. The kitchen sink did not have it, so the fix would have been unobservable there; it now does. A consumer who omits it gets a correct-but-inert rule rather than a wrong one. |

### Breakpoints and remaining physical properties

| Severity | Location | Before | After | Why |
| --- | --- | --- | --- | --- |
| LOW | `packages/tokens/src/breakpoint.json` | `sm` 40rem (640) · `md` 48rem (768) · `lg` 64rem (1024) · `xl` 80rem (1280) — the standard device-preset ladder | Keep, but say so in the docs: these are conventional starting points, and a consumer is expected to break where their own content stops fitting | The principle asks that breakpoints come from content rather than device presets. For a *design system* shipping a generic scale this is defensible — the system cannot know a consumer's content — but Container and Grid then hard-code these values, so component-level adaptation inherits device presets by default. The fix is documentation plus the container-query change above, not new numbers. |
| LOW | `popover/styles.css:305`, `:324`, `:331` | Placement modifiers named `--left` / `--right` | Add `--inline-start` / `--inline-end` variants, or document that placement is deliberately physical | In an RTL layout a popover placed `left` stays visually left rather than flipping to the reading-order-equivalent side. Physical placement names are a defensible convention (Floating UI and the CSS anchor spec both use them) — but given how thoroughly the rest of the system is logical, this reads as an oversight rather than a decision. Worth making explicit either way. |
| LOW | `code-block/styles.css:209` | `text-align: right` on the line-number gutter | `text-align: end`, with `dir="ltr"` on the code element itself | The only physical `text-align` in the system. Code is LTR even inside an RTL page, so the current behaviour is *usually* right — but it is right by accident. Setting direction on the code block and using the logical property makes the intent explicit. |

## Verified clean

- **Logical properties, essentially perfect.** Zero physical
  `margin-*`/`padding-*`/`border-*` left/right declarations across 62
  stylesheets. This is the single strongest result in any pass so far.
- **The four remaining `left:`/`right:` are legitimately physical.**
  `context-menu/styles.css:187–196` positions a menu against a **pointer
  coordinate** and flips it against `100vw`; pointer coordinates are physical
  geometry, which is exactly the exception the principle carves out.
- **The scroll affordance exists as a first-class knob.** Carousel ships
  `peek` (`--primitiv-carousel-peek`) with the inter-slide gap deliberately
  kept separate so the resting track shows no accidental peek — the hidden-content
  cue is a designed feature, not an afterthought.
- **No fixed dimensions on prose or text-flow containers.** The 134
  `height`/`block-size` declarations are on controls, icons, tracks, thumbs and
  media boxes; the only problematic ones are the six controls filed above.

## Verification

- **Direction-independence counted, not sampled.** Grepped all 62 stylesheets
  for each physical property and its logical counterpart and compared totals,
  then read every one of the six physical matches to classify it.
- **Container-query claim checked twice.** The single `@container` grep hit
  turned out to be a comment, not a query — so the count is zero, not one.
  **The count was right and the recommendation was wrong** — see the re-scoped
  row above, corrected 2026-08-18 after measuring the self-query behaviour in
  Chromium. Counting the absence of a feature is not the same as establishing
  that adding it is a stylesheet edit.
- **Control heights read individually** across Button, Input, Select, Chip,
  Badge and Tag to confirm `block-size` rather than `min-block-size`, and
  Button's `white-space: nowrap` was read in context before concluding the two
  compound.

### Still to verify

- **Nothing was pseudo-localized.** The text-growth finding is structural (a
  fixed height cannot grow); it was not confirmed by rendering a long-string
  locale, which is the proper test and would also show which labels wrap badly.
- **The 2× grouping rule was not measured.** Principle 1 asks that inter-group
  spacing be at least double intra-group spacing. That is a per-composition
  judgement rather than a token-level property, and checking it needs real
  rendered layouts rather than stylesheets.
- **No RTL render.** The grep result is strong evidence but not proof; mirroring
  the kitchen-sink with `dir="rtl"` would confirm it and catch anything the
  property audit cannot see, such as icon glyphs that should mirror.
- **No device testing for the safe-area finding** — derived from `position:
  fixed` at the viewport edge with no `env()` anywhere.

## Verdict

**Needs changes.** Three Medium and three Low, no High.

The foundation here is the best of the four passes — the logical-property
result is genuinely rare. Start with `block-size` → `min-block-size`: six
one-word changes, no visual difference in English, and it removes a whole
class of localization bug before the system has consumers who will hit it.

---

# 2026-08-15 — `better-colors`

Ten principles, checked against the generated ramps themselves — every emitted
hex converted back to OKLCH to measure hue drift, lightness spacing and chroma
distribution — plus the primitive/semantic seam and the pairs that actually
render.

**Scope:** `palette.json` (6 ramps × 10 steps × 2 themes) · `intent.json` (the
semantic tier) · 62 registry stylesheets · the emitted token layer.

## Summary

| | Count | Notes |
| --- | ---: | --- |
| High | 0 | |
| Medium | 4 | Three contrast pairs and one ramp with a measurable defect. |
| Low | 3 | Hue drift, an off-system colour set, a missing gamut branch. |
| Verified clean | 8 | The token architecture is flawless. |

**Every contrast failure has a one-step fix on a ramp that already exists** —
no engine change, no new values. And the architecture underneath scores a
result I have not seen in any pass: **zero** primitive references across 62
stylesheets against **505** semantic ones.

## Findings

### Measure the rendered pair, then report

Reported per principle 9 — the pair, its measured value, and the threshold it
misses. Colours are **not** changed here.

> **Revised after the first ramp-audit run (see the addendum below).** These
> were originally filed as route-2 re-bindings. They are better understood as
> one **engine capability gap**, and the re-binding is a mitigation rather than
> the fix.
>
> The engine is not silent on contrast. Every `Swatch` carries
> `best_foreground`, a `ForegroundSource`, and a `contrast_result` — and across
> all 100 generated swatches there is **not one `Fail`**: 72 AAA, 28 AA. The
> foreground guarantee is real and complete.
>
> But look at what `ForegroundSource` can return: `Step900`, `Step50`,
> `SoftWhite`, `SoftBlack`, `PureWhite`, `PureBlack`. It draws from the ramp's
> **ends or the white/black anchors**, because the question it answers is *"what
> text goes on this solid fill"*. It has no answer for *"which mid-ramp step is
> readable on some other surface"* — which is exactly the shape of a link
> colour, a muted-text colour, and a border.
>
> So those three roles are hand-picked in the semantic tier, with nothing
> checking them. That is why they drifted below threshold silently, and it is
> why re-binding them fixes today's values without preventing tomorrow's. The
> durable fix is to extend the engine to recommend an accessible step for a
> given (ramp, surface, threshold) and have the semantic tier consume it.

| Severity | Location | Before | After | Why |
| --- | --- | --- | --- | --- |
| MEDIUM | `intent.json` → `dark.action.link.foreground.default` | `{color.brand.500}` = `#236ce1` on `#141414` — **3.78:1**, misses 4.5:1 | `{color.brand.600}` = `#5291f9` → **5.95:1** | **This is a missed alias, not a missing dark mode.** The sibling steps *are* correctly theme-inverted: light runs `500 → 600 → 700` (darker on interaction), dark runs `500 → 300 → 200` (lighter). Only `default` was left at `500` in both themes, so every link on a dark ground renders the light-mode blue. The pattern around it shows exactly what it should have been. |
| MEDIUM | `intent.json` → `dark.content.muted` | `{color.neutral.500}` = `#747980` on `#141414` — **4.20:1**, misses 4.5:1 | `{color.neutral.600}` = `#8f949c` → **6.04:1** | This is the placeholder colour — Select's `data-placeholder` hook (added in the Combobox/Select work) keys off it, and Input uses it too. Failing by 0.30 means it looks fine in a screenshot and fails a real audit. Light mode passes at 4.71:1, so this is dark-only. |
| MEDIUM | `intent.json` → `border.default`, both themes | `{color.neutral.300}` = `#a8aeb6` on white — **2.24:1**; dark is `#565a60` on `#141414` — **2.66:1**. Both miss the 3:1 non-text threshold | `{color.neutral.400}` = `#8f949c` → **3.05:1** light | `border.default` is not decorative — it draws the boundary of Input, Select, Card and Modal, which is precisely the "boundary needed to identify the control" case WCAG 1.4.11 covers. `border.subtle` (1.79:1) is used for dividers and is legitimately exempt, so only the one role needs moving. Note this interacts with pass 01's finding that Dropdown and Modal use `border-default` where a shadow should carry the depth — fixing that one reduces the surface area of this one. |

### Hold the hue across the ramp

| Severity | Location | Before | After | Why |
| --- | --- | --- | --- | --- |
| MEDIUM | `palette.json` → `warning`, and the sRGB gamut mapping — **route 1** | Measured on the committed palette: hue drifts **38.7°** (`99.6°` yellow-green at 200 → `60.9°` orange at 800), and the light end is compressed to ΔL `+1.3`/`+2.1` against `+5.5`–`+6.7` elsewhere | See the addendum — the root cause is **not** the ramp definition | **Substantially revised.** The original diagnosis blamed the ramp curve. The ramp-audit run shows the engine's *intended* hue span is **0.0° on all ten ramps** — it holds hue perfectly by construction. The entire drift appears in the sRGB quantisation step. So the ramp definition is correct and the **gamut mapping** is what bends it, which is where any fix belongs. The compressed light end is real and stands (still ΔL 0.0 on current output). |

### Remaining

| Severity | Location | Before | After | Why |
| --- | --- | --- | --- | --- |
| LOW | `code-block/styles.css:73–79`, `:274–280` | 14 hand-authored `oklch()` syntax colours (`--primitiv-code-syntax-*`), light and dark | Either generate them from Harmoni as a real ramp, or document them as deliberately off-system | **These are the only colours in the entire system not produced by the engine.** They sit outside `palette.json`, outside the primitive/semantic tiers, and in a different notation from every other colour. Nothing is visibly wrong with them today — but if the brand hue moves, syntax highlighting will not move with it, and nothing guarantees they hold a lightness relationship with the surface they sit on. Low because a syntax theme is a defensible thing to hand-tune; it just needs to be a decision rather than an accident. |
| LOW | `palette.json` → `info` | Hue drifts **22.1°** (`199.8°` → `221.9°`), turning bluer as it darkens | Route 1, same CI caveat | Past the 15° same-colour threshold but far less severe than `warning`, and `info` is the least-used status ramp (12 aliases). Worth fixing in the same engine pass rather than on its own. |
| LOW | Emitted token layer | No `@media (color-gamut: p3)` branch anywhere; every colour ships as sRGB hex | Declare sRGB first, override inside the gamut query | The engine already has the hard part — `DisplayP3` and `oklch_to_p3_rgb` exist in `harmoni-core` and the OKLCH picker paints in P3 — but none of it reaches the emitter, so wide-gamut displays render the sRGB clamp. Low because sRGB is a correct, universally safe fallback; this is upside, not a defect. |

## Verified clean

- **The primitive/semantic seam is perfectly enforced.** **Zero**
  `var(--primitiv-color-<hue>-<step>)` references across all 62 registry
  stylesheets, against **505** semantic references
  (`surface`/`content`/`border`/`action`/`feedback`). This is the seam that
  makes theming possible and it is the single most-violated principle in most
  systems. Not violated once here.
- **One switching mechanism.** `[data-theme]` throughout; **zero**
  `prefers-color-scheme` in the token layer or the registry. The principle
  asks for one mechanism used consistently, and that is what this is. Worth
  knowing the consequence rather than treating it as a defect: the system does
  not follow the OS preference on its own, so the consumer must wire that.
- **No dead ramps.** All six are aliased by `intent.json` — neutral 58,
  brand 38, danger 32, warning/success/info 12 each. Nothing is generated for
  zero pixels.
- **Lightness spacing is correct in the direction that matters.** Every ramp
  steps *denser at the light end and looser at the dark* — neutral runs `+5.5`
  at the top to `+12.0` at the bottom, brand `+6.3` to `+12.8`. That is exactly
  the principle's requirement and it is clearly a deliberate curve.
- **Both ends stop short of pure black and white.** `neutral-50` sits at
  L 0.941 and `neutral-900` at L 0.191, so both ends can still carry hue.
- **Chroma peaks mid-ramp and falls off at both ends** on brand (0.192 at 500),
  danger (0.216 at 500), success (0.184 at 500) and warning (0.161 at 400–500).
- **No hue collisions.** Brand sits near 260°, info 200–222°, danger ~28°,
  success ~143°, warning 61–100°. Danger and primary are unambiguous side by
  side, which is the collision that actually matters.
- **Light mode passes every text pair measured** — body 18.44:1, secondary
  11.45:1, muted 4.71:1, link 4.88:1.

## A correction to pass 01

Pass 01 recommended `oklch(0 0 0 / 0.1)` for the image outline. Principle 1
warns against dropping an isolated `oklch()` into a hex codebase, and **the
token layer emits hex** — so that recommendation needs qualifying rather than
taking at face value.

It is not clear-cut in either direction: `code-block` already ships 14 `oklch()`
values, and the system is migrating to oklch anyway. The practical answer is
that the outline is a **route-3 non-palette literal**, so either notation is
correct in principle — write it to match whatever the file it lands in already
uses (`#0000001a` today), and let the migration sweep it with everything else.

## Verification

- **Ramps measured, not eyeballed.** Every emitted hex was converted back
  through linear sRGB → Oklab → OKLCH, and hue span, per-step ΔL and chroma
  distribution computed across all 6 ramps × 10 steps × 2 themes. The
  `warning` and `info` findings are arithmetic, not impressions.
- **Contrast measured on resolved pairs.** Each foreground and background was
  followed through the full alias chain to a literal before computing WCAG
  ratios, so the numbers are what actually renders — not what the token names
  suggest.
- **Candidates measured too.** Every proposed re-binding was computed before
  being offered, which is why each one names a specific step and its resulting
  ratio rather than a direction.
- **The seam was counted, not sampled** — a full grep of primitive-vs-semantic
  references across all 62 stylesheets.

### Still to verify

- **Route-1 findings are diagnosed but unverified.** The `warning` and `info`
  remedies need the engine to run, which this environment cannot do. See the
  CI note in the conventions section.
- **APCA was not used.** All measurements are WCAG 2.x contrast ratios. APCA
  models thin light-on-dark text more accurately and would likely be harsher
  on the dark-mode findings, not kinder.
- **Only the pairs listed were measured.** The `feedback.*` soft/solid tone
  pairs (Alert, Badge, Tag) and every hover/active state were not, and hover
  states are where a passing default often slips under.
- **No rendered check.** Contrast was computed from tokens; nothing was
  screenshotted and sampled, so any opacity applied at render time — notably
  the `opacity: 0.5` disabled recipe from pass 02 — is not reflected in these
  numbers and will measure worse.

## Addendum — measured against the live engine

Everything above was measured against the **committed** palette, because the
Rust toolchain is unavailable here. The `ramp-audit` workflow now runs the real
engine, and the first two runs changed several conclusions. Corrections first.

### Colour problems belong to Harmoni

The framing that makes the rest of this coherent: **Figma and the CSS are
carriers.** Neither invents a colour — both receive whatever the engine emits.
So a defect in a ramp is an engine defect, and a token-tier edit that papers
over it is a mitigation, not a fix. The three re-bindings above are worth doing
because they stop shipping failing contrast today; they are not the answer to
why those values were wrong.

### The engine holds hue perfectly — the gamut mapping does not

| ramp | theme | hue span **intended** | hue span **rendered** |
| --- | --- | ---: | ---: |
| brand | light | 0.0° | 15.6° ⚠ |
| brand | dark | 0.0° | **31.2°** ⚠ |
| danger | light | 0.0° | 6.0° |
| danger | dark | 0.0° | 6.5° |
| warning | light | 0.0° | 33.4° ⚠ |
| warning | dark | 0.0° | 22.8° ⚠ |
| success | light | 0.0° | 0.9° |
| success | dark | 0.0° | 1.6° |
| info | light | 0.0° | 6.0° |
| info | dark | 0.0° | 2.6° |

**Intended is 0.0° everywhere.** The engine's ramp definition is not the
problem — it holds hue exactly, on every ramp, in both themes. All drift is
introduced when the colour is quantised into the sRGB gamut, so the fix belongs
in the OKLCH→sRGB mapping. That is the same machinery RFC 0010 already tightened
once (the `linear_in_gamut` tolerance change from `±1e-3` to `1e-5`).

### Two corrections to my own findings

**`info` is *not* already fixed — I read the wrong column.** I filed it at 22.1°
drift, then saw current engine output at **6.0° / 2.6°** and called it fixed.
That was wrong, and it is the most important correction here.

The hue span improved because the ramp **lost 41% of its chroma** below step
500, not because the drift was corrected. A greyer ramp trivially holds hue —
there is less colour left to drift, and near-neutral steps drop out of the
measurement altogether. `info-300` goes from a vivid `#00cbd5` to a washed-out
`#80cdd8`; that is a 128/255 change in one channel.

The audit now reports light-end chroma retention alongside hue span precisely so
this cannot happen again. **Read the chroma column before acting on the hue
column** — they point in opposite directions here, and the hue number is the
flattering one.

**I missed `brand` dark entirely, and it is the worst ramp in the system at
31.2°.** My pass-05 script measured light ramps only, so a drift worse than
`warning` dark went unreported — and unlike warning, this one *does* reproduce
from the current engine, so it is live in the shipped tokens. Brand is also the
most-used ramp (38 aliases). This should outrank the original warning finding.

### The committed palette is stale for three ramps

| ramp | reproduces from its seed? |
| --- | --- |
| brand | yes (1 step differs by one unit — quantisation, not drift) |
| danger | yes (1 step, same) |
| warning | **no** — 8 steps differ materially in light, 6 in dark |
| success | **no** — 8 steps in light, 6 in dark |
| info | **no** — differs in both |

The seeds in `harmoni-seeds.json` are correct (brand and danger reproduce
exactly), so this is not a manifest error — `warning`, `success` and `info` were
generated by an older engine and never regenerated.

**Do not regenerate yet.** The current engine produces materially less chroma
below step 500, and regenerating would ship that as a visible desaturation:

| ramp | light-end chroma vs committed |
| --- | ---: |
| brand, danger | ±0% |
| success | −3% light, −1% dark |
| warning | **−21%** light, −4% dark |
| info | **−41%** light, **−15%** dark |

The pattern points straight at the cause. Steps 500–900 are **identical**; only
the steps *lighter* than the seed move. Step 500 is where the seed pins the
ramp, so this is a change in how much chroma the gamut mapping will grant above
that lightness — consistent with RFC 0010's third follow-up, which tightened
`linear_in_gamut` from `±1e-3` to `1e-5` to kill spikes in the picker's Hue
chart. That fix was correct for the chart; it appears to have also reduced
achievable chroma in the palette generator, which shares `max_in_gamut_chroma`.
The shared source of truth that RFC 0010 treats as a virtue is how the change
propagated.

So the stale palette and the desaturation are the same story: the committed
ramps are the *last good output*, and the engine regressed after them.

### The engine's accessibility guarantee is complete — within its scope

Across all 100 generated swatches: **zero `Fail` ratings, 72 AAA, 28 AA.** Every
swatch the engine produces has a guaranteed-accessible foreground, and that
guarantee holds even on the defective ramps — the warning ramp's hue drift never
produces an unreadable fill.

The limit is scope, not quality. `ForegroundSource` returns `Step900`, `Step50`,
`SoftWhite`, `SoftBlack`, `PureWhite` or `PureBlack` — ramp ends and anchors,
answering *"what text goes on this fill"*. The roles that failed (link, muted
text, border) are mid-ramp-step-on-a-different-surface, which the engine has no
API for. **That gap is the real finding behind all three contrast failures**,
and closing it would make the whole class impossible rather than fixing three
instances.

### What this changes about priority

1. **Investigate the chroma regression in `max_in_gamut_chroma`.** This now
   outranks everything else, because it blocks regeneration and therefore blocks
   every other engine fix from shipping. The `linear_in_gamut` tolerance is the
   first place to look — the picker's Hue chart and the palette generator want
   different things from the same function, and the chart won.
2. **Then regenerate**, and verify with a `ramp-audit` run that chroma retention
   is back to ±0% before committing anything.
3. **Investigate `brand` dark at 31.2°** — worst ramp, most-used ramp (38
   aliases), live in the shipped tokens, and not previously reported.
4. **Extend the engine's foreground API** to cover mid-ramp-on-surface roles, so
   the semantic tier consumes a guarantee instead of a hand-picked step.
5. **Re-bind the three failing pairs** as a stopgap while 4 is built. Safe to do
   now — it is a semantic-tier edit and does not depend on the engine.
6. **Gamut mapping** is the root cause of all *hue* drift too, so 1 and 6 may
   turn out to be one investigation.

**Do not regenerate the palette before 1.** The committed ramps are the last
good engine output; replacing them with current output trades a stale palette
for a visibly duller one.

## Verdict

**Needs changes.** Four Medium and three Low as filed, plus one previously
unreported ramp defect (`brand` dark) surfaced by the first engine run.

The token architecture remains the best result in the whole audit — the
primitive/semantic seam is not violated once in 62 stylesheets. And the engine
is in better shape than the committed palette suggests: it holds hue exactly,
and it guarantees an accessible foreground for all 100 swatches with zero
failures.

**Do not start by regenerating the palette** — which is what the first version
of this verdict recommended, on the strength of a hue-span improvement that
turned out to be chroma collapse. Start with the chroma regression in the gamut
mapping; it blocks regeneration, and regeneration blocks every other engine fix
from reaching the tokens.

The one thing safe to do immediately is re-binding the three failing contrast
pairs. That is a semantic-tier edit, it does not touch the engine, and it stops
shipping sub-threshold contrast while the rest is investigated.

---

# Pass 06 — Figma text styles

**2026-08-17.** Not a skill pass. Two findings surfaced while reading the Figma
file to build the heading `letter-spacing` variables, both about the text-style
layer rather than the tokens under it. Filed here rather than fixed, because each
is a judgement call about the file's structure.

| Severity | Location | Finding | Why it matters |
| --- | --- | --- | --- |
| MEDIUM | Figma text styles — 188 of them, `{Dense,Compact,Comfortable,Spacious} / …` | **The density prefix in a text-style name carries no information.** All four density-named variants bind the *same* Context variable: `Dense / Heading / h1` and `Spacious / Heading / h1` both bind `heading/h1/font-size`, so the two styles are identical. Density resolves from the consuming frame's Context mode, not from which style is applied. | This is exactly what modes are *for*, so the tokens are right and the style layer is 4× redundant. The cost is that the names actively mislead: a designer picking `Dense / Heading / h1` on a Comfortable frame gets Comfortable type, and nothing in the UI says so. Collapsing 188 → 47 is the obvious fix, but it is a destructive change to a shared library (every existing instance re-points), so it needs a deliberate decision rather than a sweep. |
| LOW | Figma text styles vs `registry/components/{dropdown,select,listbox,context-menu}/styles.css` | **`letter-spacing/wide` is bound nowhere in Figma.** Before this pass no text style bound `letterSpacing` at all; the four uppercase group labels in the registry bind `--primitiv-letter-spacing-wide`, so the tracking on those labels exists only in code. | A one-directional drift: the code is right (small uppercase labels need positive tracking — `better-typography` principle 8) and the Figma equivalent, `Overline`, renders untracked. Small, and it only affects the file's fidelity rather than any shipped pixel, but it is the same class of gap as the heading one just closed. |

Both were found by reading the file rather than by reasoning about it — the same
thing that caught the overlay-edge and `min-block-size` reversals earlier in this
audit.



